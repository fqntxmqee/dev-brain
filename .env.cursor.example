# === Cursor SDK 配置块（T-70） ===
# 可选：仅当 DEV_BRAIN_ADAPTER_MODE=live 且希望 cursor adapter 走 SDK 而非 cc-connect 时设置
# 获取：https://cursor.com/dashboard → Settings → API Keys

CURSOR_API_KEY=
DEV_BRAIN_CURSOR_MODEL=composer-2.5
